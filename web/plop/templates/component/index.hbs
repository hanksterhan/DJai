export * from "./{{>camelName}}";
import { {{>pascalName}} } from "./{{>camelName}}";

const TAG_NAME = "{{>tagName}}";

if (!customElements.get(TAG_NAME)) {
  customElements.define(TAG_NAME, {{>pascalName}});
}

declare global {
  interface HTMLElementTagNameMap {
    [TAG_NAME]: {{>pascalName}};
  }
}
