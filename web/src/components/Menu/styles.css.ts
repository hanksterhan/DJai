import { css } from "lit";

export const styles = css`
    .header-bar {
        display: flex;
        justify-content: space-around;
        align-items: center;
        background-color: var(--spectrum-gray-900);
    }

    sp-button {
        margin: 0 10px;
    }
`;
